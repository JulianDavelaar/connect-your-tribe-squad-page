{% render 'partials/head.liquid', title: 'Squad page' %}

  <div class="persons">
    {% for person in persons %}
      <article>
        <h2>{{ person.name }}</h2>

        {% if person.mugshot %}
          <img class="foto" src="https://fdnd.directus.app/assets/{{  person.mugshot }}?width=500">
        {% else %}
          <img src="/image-removebg-preview.png">
        {% endif %}

        <p class="nickname" >{{ person.nickname }}</p>
        <p class="geboortedatum" >{{ person.birthdate }}</p>
        <p class="residency">{{ person.residency | default: 'Woonplaats onbekend' }}</p>

        {% if person.fav_spotify_track %}
          <a href="{{ person.fav_spotify_track }}">Lievelingstrack</a>
        {% endif %}

      </article>
    {% endfor %}
  </div>


{% render 'partials/foot.liquid', squads: squads %}